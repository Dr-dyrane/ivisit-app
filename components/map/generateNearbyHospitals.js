export default function generateNearbyHospitals(userLat, userLng, count = 6, realHospitals = []) {
    // This function is now deprecated in favor of EmergencyContext logic
    // but kept as a fallback utility if needed.
    // It returns the data as-is if provided, or empty.
    
    return realHospitals || [];
}
